#include <iostream>
#include <vector>
#include<sstream>
#include <algorithm>
using namespace std;
int main(){


    
    
    cout << endl;
    return 0;
}

/**
 *      1. Sstream
 * 
 * the stream of words || tokens in the string separated by some delimiters like( ' ')
 * it jsut breaks the complete strings in tokens separeted by the ' ' and store it in a kind 
 * enumerator
 * 
 * And to access the words || tokens from the enums we have to use the ">>" to store the 
 * tokens in some of the variables and using while loop
 * 
 * 
 *      2. Inverse factorial
 * Nothing but the invFact(n) = 1/n!
 * 
 *  
 * 
 * 
*/
